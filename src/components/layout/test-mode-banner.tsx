"use client";

import { AlertTriangle } from "lucide-react";
import { isTestMode } from "@/lib/environment";

export function TestModeBanner() {
  // Banner removed - only showing "Demo" in the name when in test mode
  return null;
}
