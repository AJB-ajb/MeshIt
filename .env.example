# Supabase
# Local dev uses the dev Supabase project (meshit-dev).
# Production and preview env vars are set in Vercel.
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_DEV_PROJECT.supabase.co"
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY="your-dev-publishable-key"
SUPABASE_SECRET_KEY="your-dev-secret-key"

# NextAuth.js
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Calendar Token Encryption
# AES-256-GCM key for encrypting Google OAuth tokens at rest.
# Generate with: openssl rand -hex 32
CALENDAR_TOKEN_ENCRYPTION_KEY="your-64-char-hex-string"

# Calendar Sync Cron Secret
# Shared secret used to authenticate pg_cron → sync-all API calls.
# Generate with: openssl rand -base64 32
CALENDAR_SYNC_CRON_SECRET="your-cron-secret"

# OpenAI (for LangChain)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."

# Google AI / Gemini API
# Get from: https://aistudio.google.com/app/apikey
# Used for: Match explanation generation
GOOGLE_AI_API_KEY="..."

# Sentry Error Monitoring
# Get DSN from: https://sentry.io/settings/[org]/projects/[project]/keys/
# NEXT_PUBLIC_ prefix required — DSN is used in client-side initialization
NEXT_PUBLIC_SENTRY_DSN="https://examplePublicKey@o0.ingest.sentry.io/0"
# Auth token for source map uploads (build-time only, not needed for local dev)
# Get from: https://sentry.io/settings/account/api/auth-tokens/
SENTRY_AUTH_TOKEN="sntrys_..."
SENTRY_ORG="your-org-slug"
SENTRY_PROJECT="your-project-slug"
