# Story 1.1: Initialize Next.js Project

**Epic:** E1 - Foundation  
**Status:** ready-for-dev  
**Priority:** P0  
**Dependencies:** None  
**Blocks:** 1.2, 1.11

## Story

As a developer,
I want the Next.js 15 project initialized with pnpm, TypeScript, and Tailwind,
so that I have a working development environment.

## Acceptance Criteria

1. **Given** a fresh project directory
   **When** I run `pnpm dev`
   **Then** the app starts on localhost:3000
   **And** TypeScript compilation succeeds
   **And** Tailwind CSS is configured and working

2. **Given** the project structure
   **When** I check the configuration
   **Then** App Router is enabled
   **And** TypeScript strict mode is on
   **And** ESLint is configured

3. **Given** the package.json
   **When** I check dependencies
   **Then** Next.js 15 is installed
   **And** React 19 is installed
   **And** TypeScript 5.x is installed

## Tasks / Subtasks

- [ ] Task 1: Initialize project (AC: #1, #2, #3)
  - [ ] Run `pnpm create next-app@latest meshit --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`
  - [ ] Verify project runs on localhost:3000
  - [ ] Verify TypeScript compiles without errors

- [ ] Task 2: Configure TypeScript (AC: #2)
  - [ ] Enable strict mode in tsconfig.json
  - [ ] Add path aliases if needed

- [ ] Task 3: Verify Tailwind (AC: #1)
  - [ ] Check tailwind.config.ts exists
  - [ ] Add a test class to verify Tailwind works
  - [ ] Remove test after verification

## Dev Notes

- Use pnpm as package manager (per architecture spec)
- Next.js 15 with App Router
- Source directory structure: `src/app/`, `src/components/`, `src/lib/`

### Project Structure Notes

```
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── components/
│   └── ui/
├── lib/
│   ├── supabase/
│   └── utils/
```

### References

- [Source: planning-artifacts/architecture.md#Tech Stack]
- [Source: planning-artifacts/prd.md#7.1 Tech Stack]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
