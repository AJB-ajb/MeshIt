# Story 8.3: Dark Mode Support

**Epic:** E8 - UI/UX  
**Status:** ready-for-dev  
**Priority:** P1  
**Dependencies:** 8.1  
**Blocks:** None

## Story

As a user,
I want to switch between light and dark mode,
so that I can use the app comfortably in different lighting.

## Acceptance Criteria

1. **Given** I am using the app
   **When** I toggle dark mode
   **Then** all colors switch to dark theme
   **And** the preference is saved

2. **Given** I have a system preference
   **When** I first visit the app
   **Then** it respects my system preference

3. **Given** dark mode is enabled
   **When** I view any page
   **Then** contrast ratios meet WCAG AA standards
   **And** no elements are invisible or hard to read

4. **Given** the toggle
   **When** I click it
   **Then** the transition is smooth
   **And** there is no flash of wrong theme on load

## Tasks / Subtasks

- [ ] Task 1: Set up theme provider (AC: #1, #2, #4)
  - [ ] Install `next-themes` package
  - [ ] Create `src/components/providers/theme-provider.tsx`
  - [ ] Wrap app in ThemeProvider

- [ ] Task 2: Create theme toggle (AC: #1)
  - [ ] Create `src/components/ui/theme-toggle.tsx`
  - [ ] Use sun/moon icons
  - [ ] Add to header

- [ ] Task 3: Define dark mode colors (AC: #3)
  - [ ] Update CSS variables for dark mode
  - [ ] Ensure all components work in dark mode
  - [ ] Check contrast ratios

- [ ] Task 4: Prevent flash (AC: #4)
  - [ ] Add script to head for early theme detection
  - [ ] Use `attribute="class"` strategy

## Dev Notes

- next-themes handles SSR and hydration correctly
- Use CSS variables for seamless switching
- Test all pages in both modes

### Code Structure

```tsx
// src/components/providers/theme-provider.tsx
import { ThemeProvider as NextThemesProvider } from "next-themes";

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return (
    <NextThemesProvider
      attribute="class"
      defaultTheme="system"
      enableSystem
      disableTransitionOnChange
    >
      {children}
    </NextThemesProvider>
  );
}

// src/components/ui/theme-toggle.tsx
import { useTheme } from "next-themes";
import { Sun, Moon } from "lucide-react";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  
  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
    >
      <Sun className="h-5 w-5 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
      <Moon className="absolute h-5 w-5 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
    </Button>
  );
}
```

### References

- [Source: planning-artifacts/prd.md#NFR2 Dark Mode]
- next-themes: https://github.com/pacocoursey/next-themes

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
